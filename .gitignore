node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml

# Per-platform packages (with the native binary) are generated into dist/ by
# scripts/build-platform-packages.js at publish time — never commit.
dist/
*.tgz

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store
Thumbs.db
